import { StyleSheet } from "react-native";

export const loadingContainer = StyleSheet.create({
  style: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#fff",
  },
});
